<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_progresshuddefault"
        android:gravity="center"
        android:minWidth="@dimen/size_minwidth_progresshuddefault"
        android:orientation="vertical"
        android:padding="@dimen/padding_progresshuddefault">

        <ImageView
            android:id="@+id/ivBigLoading"
            android:layout_width="@dimen/size_image_bigloading"
            android:layout_height="@dimen/size_image_bigloading"
            android:scaleType="centerInside"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/ivSmallLoading"
            android:layout_width="@dimen/size_image_smallloading"
            android:layout_height="@dimen/size_image_smallloading"
            android:scaleType="centerInside"
            android:visibility="gone" />

        <com.nightfarmer.lightdialog.progress.CircleProgressBar
            android:id="@+id/circleProgressBar"
            android:layout_width="@dimen/size_image_smallloading"
            android:layout_height="@dimen/size_image_smallloading"
            app:roundColor="@color/roundColor_progresshuddefault"
            app:roundProgressColor="@color/roundProgressColor_progresshuddefault"
            app:roundWidth="2dip"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tvMsg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margintop_progresshuddefault_msg"
            android:gravity="center"
            android:textColor="@color/textColor_progresshuddefault_msg"
            android:textSize="@dimen/textSize_progresshuddefault_msg"
            android:visibility="gone" />

    </LinearLayout>
</LinearLayout>